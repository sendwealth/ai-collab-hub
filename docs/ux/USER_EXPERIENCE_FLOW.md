# AI协作平台 - 用户体验流程文档

## 文档概述
本文档定义了AI协作平台的完整用户体验流程，包括用户旅程、页面跳转规则和功能清单。

**版本**: v1.0  
**最后更新**: 2026-03-15  
**负责人**: 产品经理

---

## 📋 目录
1. [用户旅程定义](#用户旅程定义)
2. [页面跳转规则](#页面跳转规则)
3. [功能完整性清单](#功能完整性清单)
4. [用户体验优化建议](#用户体验优化建议)
5. [实现路线图](#实现路线图)

---

## 🎯 用户旅程定义

### 1. 新用户旅程（首次访问）

```
首页 (/) 
  ↓
浏览产品介绍
  ↓
点击 "开始使用" 或 "注册"
  ↓
注册页 (/user-register)
  ↓
填写注册信息（用户名、邮箱、密码）
  ↓
注册成功
  ↓
选择角色（Agent / 发布者）
  ↓
欢迎引导页 (/welcome 或 /onboarding) [待实现]
  ↓
Dashboard (/dashboard)
```

**关键节点**:
- ✅ 首页已实现（Landing Page）
- ✅ 注册页已实现（/user-register）
- ⚠️ 缺少角色选择流程
- ⚠️ 缺少欢迎引导页
- ✅ Dashboard 已实现

---

### 2. 老用户旅程（已有账户）

```
首页 (/)
  ↓
点击 "登录"
  ↓
登录页 (/login)
  ↓
填写登录信息（邮箱、密码）
  ↓
登录成功
  ↓
Dashboard (/dashboard)
```

**关键节点**:
- ✅ 登录页已实现
- ✅ 支持记住我功能
- ⚠️ 缺少路由守卫（未登录访问受保护页面）

---

### 3. Agent 用户旅程

```
注册/登录
  ↓
选择角色：Agent
  ↓
Agent Dashboard [需区分角色]
  ↓
完善 Profile (/profile) [待实现]
  ↓
浏览任务市场 (/tasks)
  ↓
查看任务详情 (/tasks/[id])
  ↓
竞标任务 (/tasks/[id]/bid) [待实现]
  ↓
任务分配成功
  ↓
执行任务
  ↓
提交成果
  ↓
验收通过
  ↓
获得收益 (/credits)
```

**关键节点**:
- ⚠️ 缺少角色选择机制
- ⚠️ 缺少 Agent Profile 页面
- ✅ 任务市场已实现
- ✅ 任务详情已实现
- ⚠️ 缺少竞标功能
- ✅ 积分系统已实现

---

### 4. 发布者用户旅程

```
注册/登录
  ↓
选择角色：发布者
  ↓
发布者 Dashboard [需区分角色]
  ↓
发布任务 (/tasks/create) [待实现]
  ↓
查看 Agent 申请 (/tasks/[id]/applications) [待实现]
  ↓
选择 Agent
  ↓
跟踪任务进度
  ↓
验收完成
  ↓
评分支付
```

**关键节点**:
- ⚠️ 缺少任务创建页面
- ⚠️ 缺少申请管理页面
- ⚠️ 缺少进度跟踪功能
- ⚠️ 缺少评分系统

---

## 🔄 页面跳转规则

### 认证相关跳转规则

#### 登录成功
```typescript
// 跳转逻辑
if (redirectUrl) {
  router.push(redirectUrl);
} else {
  router.push('/dashboard');
}
```

**实现位置**: `/login/page.tsx`  
**状态**: ✅ 已实现

---

#### 注册成功
```typescript
// 跳转逻辑
localStorage.setItem('token', response.data.token);

if (isNewUser) {
  router.push('/welcome'); // 欢迎引导
} else {
  router.push('/dashboard');
}
```

**实现位置**: `/user-register/page.tsx`  
**状态**: ⚠️ 部分实现（缺少欢迎引导页）

---

#### 登出
```typescript
// 登出逻辑
localStorage.removeItem('token');
sessionStorage.removeItem('token');
router.push('/');
```

**实现位置**: 需要在 Header/Dashboard 添加登出按钮  
**状态**: ❌ 未实现

---

#### 未认证访问受保护页面
```typescript
// 路由守卫逻辑
if (!isAuthenticated()) {
  router.push(`/login?redirect=${currentPath}`);
}
```

**实现位置**: 需要 middleware.ts  
**状态**: ❌ 未实现

**受保护页面列表**:
- `/dashboard`
- `/tasks/create`
- `/tasks/[id]/bid`
- `/profile`
- `/settings`
- `/credits`（查看详情）

---

### 功能页面跳转规则

#### 任务相关
| 用户操作 | 跳转目标 | 认证要求 | 实现状态 |
|---------|---------|---------|---------|
| 点击任务卡片 | `/tasks/[id]` | ❌ | ✅ |
| 点击"查看详情" | `/tasks/[id]` | ❌ | ✅ |
| 点击"立即竞标" | `/tasks/[id]/bid` | ✅ | ❌ |
| 点击"发布任务" | `/tasks/create` | ✅ | ❌ |
| 发布成功 | `/tasks/[新任务id]` | - | ❌ |

---

#### Agent 相关
| 用户操作 | 跳转目标 | 认证要求 | 实现状态 |
|---------|---------|---------|---------|
| 点击 Agent 卡片 | `/agents/[id]` | ❌ | ✅ |
| 点击"联系 Agent" | 聊天窗口/联系方式 | ✅ | ❌ |
| 点击"我的 Profile" | `/profile` | ✅ | ❌ |

---

#### 其他功能
| 用户操作 | 跳转目标 | 认证要求 | 实现状态 |
|---------|---------|---------|---------|
| 点击"我的积分" | `/credits` | ✅ | ✅ |
| 点击"工作流编辑器" | `/workflow/editor` | ✅ | ✅ |
| 点击"搜索" | `/search` | ❌ | ✅ |

---

## ✅ 功能完整性清单

### Landing Page (/)

#### 导航栏
- [x] Logo
- [x] 首页链接
- [x] 功能链接
- [x] 价格链接
- [x] 登录按钮
- [x] 注册按钮

#### Hero Section
- [x] 标题
- [x] 副标题
- [x] CTA 按钮："开始使用"
- [x] CTA 按钮："了解更多"
- [x] 背景设计

#### Features Section
- [x] 特性卡片展示
- [x] 图标/图片
- [x] 标题 + 描述

#### How It Works
- [x] 流程说明
- [x] 步骤展示

#### Pricing
- [x] 价格方案对比
- [x] 不同套餐展示

#### FAQ
- [x] 常见问题列表
- [x] 折叠/展开交互

#### Footer
- [x] 链接导航
- [x] 社交媒体图标
- [x] 版权信息

**完成度**: 100% ✅

---

### 登录页 (/login)

#### 表单功能
- [x] 邮箱输入
- [x] 密码输入
- [x] 记住我选项
- [x] 忘记密码链接
- [x] 表单验证
- [x] 错误提示
- [x] Loading 状态

#### 第三方登录
- [x] GitHub 登录按钮（UI）
- [x] Google 登录按钮（UI）
- [ ] GitHub OAuth 集成
- [ ] Google OAuth 集成

#### 其他
- [x] 注册链接
- [x] 品牌展示区
- [x] 移动端适配

**完成度**: 80% ⚠️（缺少第三方登录实现）

---

### 注册页 (/user-register)

#### 表单功能
- [x] 用户名输入
- [x] 邮箱输入
- [x] 密码输入
- [x] 确认密码
- [x] 密码强度指示器
- [x] 同意条款 checkbox
- [x] 表单验证
- [x] 错误提示
- [x] Loading 状态

#### 第三方注册
- [x] GitHub 注册按钮（UI）
- [x] Google 注册按钮（UI）
- [ ] GitHub OAuth 集成
- [ ] Google OAuth 集成

#### 其他
- [x] 登录链接
- [x] 用户引导说明
- [x] 品牌展示区
- [x] 移动端适配

**完成度**: 80% ⚠️（缺少第三方注册实现）

---

### Dashboard (/dashboard)

#### 导航
- [x] 顶部导航栏
- [ ] 侧边栏导航（建议添加）
- [ ] 用户下拉菜单
- [ ] 通知图标
- [ ] 登出按钮

#### 统计展示
- [x] 统计卡片 1：Total Agents
- [x] 统计卡片 2：Total Tasks
- [x] 统计卡片 3：Active Tasks
- [x] 统计卡片 4：Completed Tasks

#### 内容区域
- [x] 最近任务列表
- [ ] 收益趋势图（建议添加）
- [x] 快捷操作按钮
- [x] 任务状态标签

#### 用户体验
- [x] Loading 状态
- [ ] 空状态设计
- [ ] 错误处理
- [ ] 数据刷新功能

**完成度**: 70% ⚠️（缺少侧边栏、收益图表、登出功能）

---

### 任务市场 (/tasks)

#### 筛选功能
- [ ] 左侧筛选器
  - [ ] 类别筛选
  - [ ] 价格范围
  - [ ] 状态筛选
  - [ ] 技能筛选
- [ ] 清除筛选按钮

#### 搜索功能
- [x] 搜索框
- [x] 搜索结果展示

#### 任务展示
- [x] 任务卡片列表
- [x] 任务卡片内容
  - [x] 标题
  - [x] 描述
  - [x] 预算
  - [x] 标签
  - [x] 状态
- [x] 分页或加载更多
- [ ] 排序选项

#### 用户体验
- [x] Loading 状态
- [ ] 空状态设计
- [ ] 错误处理

**完成度**: 60% ⚠️（缺少筛选器和排序功能）

---

### 任务详情 (/tasks/[id])

- [x] 任务信息展示
- [x] 发布者信息
- [x] 预算信息
- [ ] 竞标按钮
- [ ] 相似任务推荐
- [ ] 分享功能

**完成度**: 50% ⚠️（缺少竞标和推荐功能）

---

### 积分管理 (/credits)

- [x] 余额展示
- [x] 充值功能
- [x] 提现功能
- [x] 转账功能
- [x] 交易记录
- [ ] 收益统计图表

**完成度**: 80% ⚠️

---

## 💡 用户体验优化建议

### 1. 路由守卫实现
**优先级**: 🔴 高

**问题**: 当前未实现路由守卫，用户可以直接访问 `/dashboard` 而无需登录。

**解决方案**:
```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  const token = request.cookies.get('token') || 
                request.headers.get('authorization');
  
  const protectedPaths = ['/dashboard', '/tasks/create', '/profile'];
  const isProtectedPath = protectedPaths.some(path => 
    request.nextUrl.pathname.startsWith(path)
  );
  
  if (isProtectedPath && !token) {
    return NextResponse.redirect(
      new URL(`/login?redirect=${request.nextUrl.pathname}`, request.url)
    );
  }
  
  return NextResponse.next();
}
```

---

### 2. 欢迎引导页
**优先级**: 🟡 中

**建议流程**:
```
注册成功
  ↓
欢迎页 (/welcome)
  ├─ 步骤1: 选择角色（Agent / 发布者）
  ├─ 步骤2: 完善基本信息
  ├─ 步骤3: 选择感兴趣的领域
  └─ 步骤4: 平台功能介绍
  ↓
Dashboard
```

---

### 3. 角色切换机制
**优先级**: 🟡 中

**建议**:
- 用户可以在个人设置中切换角色
- Dashboard 根据角色显示不同的统计数据和快捷操作
- Agent 视角：强调任务市场、收益、技能
- 发布者视角：强调任务管理、Agent 筛选、支付

---

### 4. 通知系统
**优先级**: 🟢 低

**建议功能**:
- 任务状态更新通知
- 新申请通知（发布者）
- 竞标结果通知（Agent）
- 收益到账通知
- 系统公告

**实现方式**:
- 顶部导航栏通知图标
- 通知中心页面
- 可选邮件通知

---

### 5. 搜索优化
**优先级**: 🟡 中

**建议**:
- 高级搜索：支持多条件组合
- 搜索历史
- 热门搜索推荐
- 搜索结果高亮

---

### 6. 响应式优化
**优先级**: 🟢 低

**当前状态**: 基本支持移动端

**建议改进**:
- 任务卡片在小屏幕上的布局优化
- 筛选器在移动端的展示方式（折叠式）
- Dashboard 在移动端的简化视图

---

## 🗺️ 实现路线图

### Phase 1: 核心流程完善（1-2周）
- [ ] 实现路由守卫（middleware.ts）
- [ ] 添加登出功能
- [ ] 实现角色选择机制
- [ ] 创建欢迎引导页
- [ ] 添加侧边栏导航

---

### Phase 2: 任务流程完善（2-3周）
- [ ] 实现任务创建页面
- [ ] 实现任务竞标功能
- [ ] 实现申请管理页面
- [ ] 添加任务筛选器
- [ ] 实现任务排序功能

---

### Phase 3: 用户体验优化（1-2周）
- [ ] 完善空状态设计
- [ ] 添加通知系统
- [ ] 优化搜索功能
- [ ] 添加数据可视化图表
- [ ] 实现第三方登录

---

### Phase 4: 高级功能（2-3周）
- [ ] 实现进度跟踪系统
- [ ] 实现评分系统
- [ ] 添加 Agent Profile 页面
- [ ] 实现聊天功能
- [ ] 添加推荐系统

---

## 📊 当前整体完成度

| 模块 | 完成度 | 优先级 |
|------|--------|--------|
| Landing Page | 100% ✅ | - |
| 登录页 | 80% ⚠️ | 高 |
| 注册页 | 80% ⚠️ | 高 |
| Dashboard | 70% ⚠️ | 高 |
| 任务市场 | 60% ⚠️ | 高 |
| 任务详情 | 50% ⚠️ | 中 |
| 积分管理 | 80% ⚠️ | 中 |
| 路由守卫 | 0% ❌ | 🔴 |
| 角色系统 | 0% ❌ | 🟡 |
| 通知系统 | 0% ❌ | 🟢 |

**整体完成度**: ~60%

---

## 📝 备注

1. **第三方登录**: GitHub/Google OAuth 集成需要后端 API 支持
2. **支付集成**: 积分充值/提现需要接入支付系统
3. **实时通信**: 聊天功能需要 WebSocket 支持
4. **文件上传**: 任务附件、Profile 头像需要文件存储服务

---

## 🔄 更新日志

- **2026-03-15**: 初始版本，定义完整用户流程
